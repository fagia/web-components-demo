# WEB COMPONENTS DEMO

## Requirements

Docker

## Install/update

    docker-compose build --force-rm

## Run

    docker-compose up

## Stop

Kill the docker-compose process with `Ctrl+c`, then:

    docker-compose down
